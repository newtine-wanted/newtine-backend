/**
 * @packageDocumentation
 * @module api.functional.feed
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
//================================================================
import type { IConnection } from "@nestia/fetcher";
import { PlainFetcher } from "@nestia/fetcher";

import type { FeedRequest } from "../../../../apps/api/src/issue/type/feed.request";
import type { FeedResponse } from "../../../../apps/api/src/issue/type/feed.response";

/**
 * @security bearerAuth
 * @security guestFeedCookie
 * @security
 * @throws 400 The request is invalid.
 * @throws 401 Authentication is required.
 * @throws 404 The requested resource was not found.
 * @throws 409 The request conflicts with the current resource state.
 * @throws 410 The requested resource is no longer available.
 * @throws 503 The service is temporarily unavailable.
 * @controller FeedController.getFeed
 * @path GET /api/feed
 * @accessor api.functional.feed.getFeed
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function getFeed(
  connection: IConnection,
  request: getFeed.Query,
): Promise<getFeed.Output> {
  return PlainFetcher.fetch(connection, {
    ...getFeed.METADATA,
    template: getFeed.METADATA.path,
    path: getFeed.path(request),
  });
}
export namespace getFeed {
  export type Query = FeedRequest;
  export type Output = FeedResponse;

  export const METADATA = {
    method: "GET",
    path: "/api/feed",
    request: null,
    response: {
      type: "application/json",
      encrypted: false,
    },
    status: 200,
  } as const;

  export const path = (request: Query) => {
    const variables: URLSearchParams = new URLSearchParams();
    for (const [key, value] of Object.entries(request as any))
      if (undefined === value) continue;
      else if (Array.isArray(value))
        value.forEach((elem: any) => variables.append(key, String(elem)));
      else variables.set(key, String(value));
    const location: string = "/api/feed";
    return 0 === variables.size
      ? location
      : `${location}?${variables.toString()}`;
  };
}
