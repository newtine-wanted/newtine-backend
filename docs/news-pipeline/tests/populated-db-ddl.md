# 기존 데이터 보존 DDL 검증 (2026-09-20)

- 영속 로컬 PostgreSQL의 기존 서비스 테이블 31개에 대해 적용 전후 행 수와 정렬된 전체 행 JSON의 MD5를 비교했다. 모두 일치했다. 검증 DB의 issues 161행(테스트 fixture 포함)도 그대로 유지됐다.
- `001_schema.sql`, `002_seed_terms.sql`, `004_seed_search.sql`을 각각 두 번 적용했다. 모두 성공했다.
- 기존 테이블은 조회만 하며 새 파이프라인 테이블·인덱스만 생성한다. 용어는 기존 정의를 덮어쓰지 않고 검색 초기화는 파이프라인 복사본만 교체한다.
- 검색 초기 복사 결과가 DB 현재 시각 기준 최근 7일 공개 이슈 조건과 일치함을 확인했다. 이 검증 시점의 대상은 0건이었다. 실제 검색 데이터의 수정·비공개·삭제 동기화는 search-copy.md의 별도 fixture 검증을 따른다.
- 운영 DB 적용 및 유료 AI 호출 없음.
