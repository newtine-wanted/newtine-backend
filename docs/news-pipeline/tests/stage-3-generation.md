# 3단계 생성 검증

- 날짜: 2026-09-20
- 상태: 사용자 검토 대기
- 세대 테이블은 추가하지 않았다. 기존 AGE_19_34 / AGE_35_49 / AGE_50_64 / AGE_65_PLUS 코드를 사용한다.

## 시나리오 및 결과

| 시나리오 | 결과 |
| --- | --- |
| 관점 null·0개·1개여도 생성 계속 | 단위 테스트와 실제 관점 1개 표본에서 통과 |
| 본문 2개 미만 | 생성 LLM을 호출하지 않고 INSUFFICIENT_BODIES 기록 |
| 기존 용어 재사용·새 용어만 호출 | 단위 및 로컬 DB 검증 통과 |
| DB 밖 분류 코드·알 수 없는 근거 ID | 거부 확인 |
| 중요도 가중치·0~1 포화, 최신성 72시간 반감 | 통과 |
| 불명확·미래 사건 시각 | 선정 기사 최초 보도 시각으로 대체 |
| 본문 실패 기사까지 포함한 최초 보도 시각 | 단위 검증 통과 |
| 후속 추적 기간·검색어 범위 | 통과 |
| 실패 후 재개 | 완료한 본문·생성·분류 반복 없이 용어 생성부터 재개 |
| 실패 호출 사용량 | 보존 확인 |
| 완료 실행 재요청 | 새 외부 호출 없이 동일 실행 반환 |
| 동시 실행·만료 소유자 접근 | 거부 및 소유권 교체 확인 |
| 공개 이슈·새 용어 사전 쓰기 | 발생하지 않음 |
| 캐시 할인·토큰 누락 | 할인 계산 및 비용 불완전 표시 확인 |

## 실행 명령

- `npm run test:jest -- test/unit/newsGeneration.test.ts test/unit/newsDiscovery.test.ts test/unit/newsCollection.test.ts`: 최종 43개 통과 (생성 11개, 탐색 16개, 수집 16개).
- `npm run build:batch`, `npm run typecheck`, 변경 파일 ESLint 통과.
- `node scripts/news-generation-local.mjs smoke`: 영속 로컬 PostgreSQL + 외부 호출 mock 통합 검증 통과.
- 로컬 DB: 기존 운영 동일 기본 스키마·공통 마이그레이션·1/2단계 마이그레이션에 `Migration20260920000200NewsGeneration` 추가. `news_terms`, `news_generation_runs` 생성. 기존 API worker는 변경하지 않았다.
- 통합 검증 실행 ID: `01a0bb00-28dc-756b-ae57-44d772193fe5`.

## 실제 기사 표본

- 직전 2단계 실행 `01a0baeb-064d-7593-8abd-6cefbf376dc2`의 첫 2개 선정 이슈를 사용했다. 후보 상한 3개로 전체 탐색을 다시 실행한 결과는 아니다.
- 원본 실행 기록과 완료 여부를 바꾸지 않도록 별도 로컬 JSON 체크포인트에 표본을 실행했다. DB 통합 검증은 위 smoke에서 별도로 수행했다.
- 본문 수집 10건 중 9건 성공. 생성 2개 성공, 관점 각각 1개·2개. 첫 이슈의 관점 부족을 이유로 중단하지 않았다.
- LLM 6회, 입력 65,188 / 출력 1,930 토큰, 추정 비용 $0.057576 USD. 캐시 토큰 0, 단가 입력 $0.75 / 캐시 입력 $0.075 / 출력 $4.50 per 1M. 세금·환율·다른 실행 비용 제외.
- 원본: `.local/news-generation/preview-2-issues.json`
- 보고서: `.local/news-generation/preview-2-issues-report.md`

## 남은 문제 및 다음 단계

- 실제 표본에 모든 세대가 관련 있다고 선택하면서 각 세대의 직접 영향은 없다고 쓰는 불일치가 있다. 다른 표본에는 관점 주체가 ‘전문가’로 모호하게 표현됐다. 4단계 파생 데이터 간 모순·UX 라이팅·근거 일치 검증 대상으로 남긴다.
- 본문 수집 실패는 외부 언론사 응답에 따라 발생할 수 있다. 선정된 기사 외 추가 검색·대체 기사 수집은 하지 않는다.
- LLM 사실 정확성은 위 형식·코드·범위 테스트 통과만으로 보장하지 않는다. 이번 결과는 검증·공개 전 초안이다.
- 기존 로컬 DB에 활성 인물·기관·정당 데이터가 없어 실제 표본의 entity 분류는 비어 있다. 허용 목록 제한은 단위 테스트로 확인했다.
- 운영 스케줄러·권한·네트워크는 검증하지 않았다. 4단계 의미 검증과 5단계 저장·공개는 미구현이다.

## 관점 2개 고정 변경 검증

- 이전 nullable·1개 허용 정책은 사용자 최신 지시로 대체했다.
- JSON schema의 minItems/maxItems=2, 런타임에서도 정확히 2개 및 서로 다른 이해관계자·유효한 근거 ID를 검사한다.
- 기존 경계 테스트를 0/1/3개 거부와 2개 통과로 수정했다.
- 1~3단계 단위 테스트 43개, 배치 빌드, 로컬 DB smoke 통과.
- DB 검증 ID: `01a0bb0b-98f3-7758-bc64-7c3e1129a2a8`. 전체 재실행 전 실행 기록 백업에 포함했다.

## 관점 2개 기준 전체 재실행 (2026-09-20)

- 1단계: `01a0bb0b-ff09-75da-b1b2-cc0128401a28`. 검색어 34개, 추출 92개(각 최대 3개), 중복 정리 후 82개.
- 2단계: `01a0bb0d-7c71-736e-8d59-592731ce4e4b`. 선정 57개, 기사 부족 25개, 기존 공개 이슈 중복 0개. 선정 기사 265건. 현재 로컬 설정의 언론사 우선순위 1/2/3을 사용했다.
- 3단계: `01a0bb0f-3ebd-774e-bf30-eaebe1000c98`. 생성 57개, 본문 부족 제외 0개. 본문 성공 246건 / 실패 19건(이슈별 합계).
- 모든 생성 이슈에 서로 다른 이해관계자 관점 정확히 2개, 총 114개를 확인했다. 각 관점의 기사 ID가 해당 이슈의 확보 본문을 참조하는지 확인했다. 점수 0~1 범위와 본문 2~5개 조건도 통과했다.
- 1단계: 35회 / 입력 90,473 / 출력 3,649 / 추정 $0.08427525.
- 2단계: 78회 / 입력 182,286 / 출력 3,012 / 추정 $0.15026850.
- 3단계: 171회 / 입력 1,263,536 / 출력 69,182 / 추정 $1.25897100 (캐시 토큰 0).
- 총 입력 1,536,295 / 출력 75,843 토큰, **$1.49351475 USD**. 1·2단계 캐시 미기록분은 일반 입력 단가로 계산, 세금·환율·이전 실행·개발 테스트 제외.
- 실행 오류·재시도 없이 완료했다. 기존 공개 이슈 비교 대상은 없어 해당 LLM 중복 비교를 생략했다.
- 실행 전 원본 DB 실행 기록은 `.local/archive/before-three-stage-rerun/runs.sql`로 백업하고 실행 테이블 3개만 초기화했다.
- 보고서: `.local/news-pipeline-results.md`. 통합 보고서 스크립트: `scripts/news-pipeline-report.mjs`.
- 형식·개수·출처 ID 확인이며 기사와 생성 문장 사이 의미 일치 및 UX 라이팅 품질 보증은 아니다. 4단계 검증·5단계 공개는 수행하지 않았다.

## 후보 제외·공통 조건부 영향·프롬프트 분리 검증 (2026-09-20)

- 공통 조건부 영향의 4세대 설명·근거 통일 및 유효하지 않은 근거 거부 확인.
- 단계 단위 테스트 46개, 전체 단위 테스트 280개, 일반 통합 테스트 11개 통과. `REPORT_TEST_DATABASE_URL`이 없어 별도 report_test DB가 필요한 9개는 건너뜀.
- 뉴스 파이프라인 실제 로컬 DB smoke 1~3단계 모두 통과. 배치 빌드·타입 검사·변경 코드 ESLint 통과.
- 1~3단계 LLM 지시문 9개는 `prompts/news-pipeline`의 Markdown으로 분리했고 실제 호출 코드가 이를 읽는다.

## 분리된 프롬프트·후보 제외·공통 영향 전체 실행 결과 (2026-09-20)

- 1단계 ID: `01a0bc38-9449-750f-9a39-75d352f5d56a`. 34개 검색어 → 94개 후보 → 제외 6개 및 중복 통합 후 73개. 제외 사유는 IRRELEVANT 5개, HYPERLOCAL 1개다.
- 2단계 ID: `01a0bc3c-d8c0-765d-8075-76aaaaae73ce`. 선정 55개, 기사 부족 18개. 선정 기사 231건.
- 3단계 ID: `01a0bc3e-9d63-7357-a94f-596770936133`. 생성 55개, 본문 부족 제외 0개. 본문 확보 209건, 실패 22건(이슈별 합계).
- 공통 조건부 영향 42개에서 4개 세대의 설명·근거 ID가 동일함을 확인했다. 나머지 13개는 개별 세대 설명을 사용한다. 모든 생성 이슈의 관점 2개·서로 다른 이해관계자·해당 본문에 속하는 근거 ID를 확인했다.
- 자동화 테스트 최종 결과: 단위 281개, 통합 11개 통과. 별도 `REPORT_TEST_DATABASE_URL` 및 `report_test` DB가 필요한 기존 리포트 테스트 9개는 건너뜀. 뉴스 전용 DB smoke 1~3단계는 별도로 모두 통과했다.
- 기사별 후보 추출은 재호출하지 않았다. 최종 정리 1회 오류 및 번호/제목 보완 전후 호출은 1단계 사용량에 포함했다. 보완 전에 중단한 2단계 스냅샷은 `.local/archive/filter-correction-collection.json`에 저장하고 비용에 별도 합산했다.
- 비용: 1단계 $0.09294225, 최종 2단계 $0.11163825, 3단계 $1.23177450, 중단된 2단계 시도 $0.08814450. 합계 **$1.52449950 USD**. 세금·환율·이전 세션 실행 제외, 캐시 미기록 입력은 일반 단가 적용.
- 보고서: `.local/news-pipeline-results.md`. 9개 프롬프트: `prompts/news-pipeline/README.md` 참고.
- 남은 품질 검토: 일부 공통 영향은 ‘사람이라면’ 형태의 조건·영향 대신 일반적인 사건 설명으로 생성됐다(예: 김건희 2심 선고, 유엔총회 외교전). 구조적 통일은 확인했지만 문장의 의미·조건 적절성은 별도 검토가 필요하다. 제외 목록에도 정책 관련성 판단이 모호한 사례가 있어 보고서에서 전체 6개를 공개한다. 4단계 의미 검증 및 5단계 공개는 수행하지 않았다.

## 용어 최대 3개 제한
- 생성 프롬프트·JSON schema·런타임 검사 상한을 3개로 통일했다. 4개 용어 응답 거부 테스트 추가.
- 이미 생성된 이번 실행 초안은 원본을 유지하고 4단계 최종 사본에서 앞 3개 및 대응 설명만 유지한다.

## 요약 본문 1~2문장 제한

- 생성 프롬프트와 응답 schema 설명에 짧은 1~2문장 요구를 추가했다. 핵심요약 3줄은 유지한다.
- 생성/검증의 공통 문장 수 검사와 수정 프롬프트에 같은 제한을 적용했다. 소수점·인용부호를 포함한 1~2문장 허용, 3문장 거부 및 검증 실패 필드 지정 회귀 테스트 통과.
- 생성·검증 단위 테스트 36개, 타입 검사·batch 빌드·변경 코드 ESLint 통과.
- 기존 기사 선별 결과 40개를 이용한 3단계 단독 실제 재실행 완료. 기존 3·4단계 DB 기록은 백업했고 4단계는 재실행하지 않는다.

## 요약 축약 실제 재실행 결과 (2026-09-20)

- 기존 2단계 실행 `01a0bc9b-1a75-7428-bb37-3c7aeb9d89c3`의 선정 이슈 40개로 3단계만 재실행했다.
- 새 생성 실행: `01a0bca7-f50e-7070-9960-024ecd7b0a70`. 40개 생성 완료, 본문 부족 0개. 확보 본문 155건/수집 실패 14건(이슈별 합계).
- 40개 모두 요약 본문 2문장. 평균 2.975 → 2.000문장, 공백 포함 평균 183.4 → 103.9자로 약 43.3% 감소했다. 핵심요약 3줄, 관점 2개, 용어 최대 3개도 확인했다.
- 120회 호출, 입력 854,974 / 출력 47,874 토큰, 캐시 할인 적용 추정 **$0.84266670 USD**. 사용량 누락 없음. 이번 3단계 실행만의 비용이며 앞선 전체 실행 비용과 세금·환율은 제외한다.
- 1·2·4단계는 재실행하지 않았다. 새 출력에는 이전 4단계의 통과·보류 판정을 적용하지 않는다. 새 1단계 주제 외 후보 1개 제한은 다음 탐색부터 적용한다.
- 로컬 결과: `.local/news-short-summary-preview.md`(제목·요약 전체), `.local/news-short-summary-results.md`(분량·비용·요약), `.local/runs/short-summary-20260920T023154Z`(JSON·로그·원본 백업).
