# 검증 용어 저장·검색 인덱스 제거 검증 (2026-09-20)

- 타입 검사, 배치 빌드, lint 통과. 뉴스 단위 테스트 81개 통과.
- 영속 로컬 PostgreSQL 검증 스모크 통과: PASSED 최종 용어 저장, 다음 생성의 DATABASE 조회, 기존 정의 보존, 같은 실행 재사용, HELD 용어 제외, 소유권 상실 시 저장 방지.
- 용어 삽입 실패를 DB 제약 위반으로 유도: 앞서 삽입한 용어와 실행 완료 상태가 함께 롤백됨을 확인.
- 초기 테스트는 본문에 없는 fixture 용어가 검증 과정에서 제거되어 실패했다. 본문에 해당 용어를 포함하도록 fixture를 수정한 후 통과했다.
- 로컬 `news_collection_published_title_trgm` 삭제 완료. 인덱스 없는 상태에서 수집 스모크 통과: 유사도 상위 10개 조회 및 중복/기사 부족/선정 처리 정상.
- 유료 AI 호출 없음. 기존 완료 실행의 용어 소급 저장은 수행하지 않음.
