# 1단계: 대상 이슈 탐색 및 선정

- 상태: `PLANNED`
- 작성: 2026-09-20
- 사용자 검토: 검색 카테고리 승인: 세대 제외, 검색어별 기사 50건 (2026-09-20)

## 기존 구현과 변경 방향

기존 PipelineWorker는 단일 검색어로 기사 20건을 검색하고, 최근 이슈 최대 100개와 함께 LLM에 입력해 후보 선정과 중복 판단을 한 번에 수행한다. 이어서 콘텐츠 생성까지 실행한다.

이번 구현은 별도 1단계 명령으로 실행한다. 기존 API·워커·공통 서버 구조를 유지하고, 검토 가능한 후보를 전용 테이블에 저장한다. 공개 이슈 생성 및 기존 이슈와 중복 비교는 이후 단계다.

사용자 지시(2026-09-20): 검색어별 제목만 LLM에 입력하여 후보 선정, 전체 후보를 모아 후보 간 중복 검증. Google Trends는 추후 개발로 이관하며 관련 구현은 제거한다. direction 폴더는 수정하지 않는다.

## 구현 범위와 방법

1. DB 토픽·지역·활성 정치 주체를 독립 검색어로 변환. 세대 검색은 사용자 지시로 제외.
2. 네이버 최신순 검색. 일반 탐색은 실행 기준 최근 24시간, 후속 탐색은 마지막 확인 이후 기사. 날짜 누락/미래 날짜 제외. URL 및 정규화 제목으로 중복 제거, 원문·설명은 LLM에 보내지 않음.
3. 검색어별 제목 배열만으로 정치·공공정책 관련 구체적인 사건 후보 추출. 근거 제목의 배열 인덱스로 로컬 기사 메타데이터 연결. 모델은 gpt-5.4-mini-2026-03-17 고정.
4. 추적 중이며 기한이 남은 이슈에 한해 이슈명·지정 검색어 검색. 후보를 기존/이전 후속 제목과 비교해 명백한 새 전개만 남기고 불확실한 결과 보류. 부모 이슈 ID 보존.
5. 후보 전체의 제목 기반 중복 그룹 검증. 대표 후보 및 모든 근거/검색어/부모 연결 보존. 기존 게시 이슈 비교(pg_trgm)는 2단계에서 구현.
6. 일자별 실행·검색 결과 체크포인트·실패 기록·완료 후보 영속화. 동시 실행 및 당일 중복 성공 방지. 실패 후 재실행 가능. 실패 시 후속 확인 시각을 전진시키지 않음.

## 검색 범위 (사용자 승인)

- 토픽: DB 표시명을 `·`로 나누어 개별 검색(주거, 일자리, 세금, 금융, 복지, 연금, 교육, 보건, 의료, 환경, 기후, 외교, 안보, 지역, 교통, 정치, 사법).
- 지역: DB regions.name 개별 검색, 17개 표준 시도.
- 주체: DB entities 중 활성 POLITICIAN, INSTITUTION, PARTY 이름.
- 세대: 제외.
- 검색어별 기사 최대 50개, 후보 최대 5개. 전체 검색/후보 상한은 초과 시 조용히 누락하지 않고 실패로 기록. 설정 파일로 조정.

## 변경 예정 파일

- apps/batch/src/discovery/*: 타입, 정책, 저장소, LLM, 실행 서비스, 전용 마이그레이션
- scripts/news-discovery.mjs: 독립 실행/마이그레이션/설정 미리보기 명령
- config/news-discovery.json: 검색 정책 및 제한
- package.json: 실행 명령
- test/unit/newsDiscovery*.test.ts, scripts/news-discovery-smoke.mjs
- docs/news-pipeline/tests/stage-1-discovery.md

## 완료 조건

- 제목만 전송, 날짜/중복/검색 제한, 후보 중복 및 후속 분기 자동 테스트 통과.
- 기존 공통 기본 스키마와 마이그레이션을 적용한 별도 로컬 PostgreSQL에서 실행·실패 복구·영속화 검증.
- 네이버·OpenAI 외부 API 실행 가능 여부와 운영 전용 검증 항목 분리 기록.
- 구현 및 검증 결과를 기록하고 사용자 검토 요청. 운영 스케줄 시간 확정/등록은 별도 검토.

## 제외 범위 및 현재 진행 상태

- Google Trends: 사용자 요청과 수정된 direction.md에 따라 추후 개발. CSV 자동 다운로드·파서·검색어 주입·전용 타입·실행 명령·환경변수·테스트·추가 의존성·로컬 수집 파일을 제거했다.
- 이번 범위는 카테고리 기반 네이버 뉴스 탐색과 후속 보도 탐색이다. 세대 검색은 제외한다.
- 나머지 1단계 코드는 구현 진행 중이다. 로컬 DB 기본 스키마 및 마이그레이션 적용까지 확인했으며, 전체 DB end-to-end와 실제 API 검증·사용자 최종 검토는 아직 완료하지 않았다.
