# 4단계: 생성 결과 검증

- 상태: `REVIEW_PENDING` — 검증 범위 축소 구현·자동화 검증 완료
- 작성: 2026-09-20

## 기존 구현 검토

- `libs/core/src/pipeline/domain/pipeline.validator.ts`는 요약·관점·용어·세대별 영향의 형식과 기사 ID를 검사한다. 현재 3단계의 제목·사건 시각·분류 코드·점수·관점 정확히 2개·공통 조건부 영향은 모두 검증하지 않는다.
- 기존 의미 검증은 기사 내용 일치 외에 서로 겹치지 않는 독립 취재 근거 그룹을 최소 2개 요구한다. 같은 보도자료를 옮긴 여러 기사는 독립 근거 2개를 충족하지 못할 수 있다. 독립성이 불확실해도 통과하지 않는다.
- direction.md의 4단계에는 독립 근거 개수 조건이 없다. 이 조건을 유지하면 내용이 기사와 일치하는 이슈도 보류될 수 있고, 제외하면 기존 검증보다 통과 기준이 완화된다. 구현 전 사용자 결정을 받는다.
- 기존 worker는 검증 실패 시 오류 처리하며, 실패 항목만 수정하는 흐름은 없다. 새 파이프라인은 독립 실행 모듈에서 항목별 재생성을 구현하고 기존 worker와 공개 API를 변경하지 않는 안을 제안한다.
- 기존 생성 자료형과 현재 3단계 자료형도 다르므로 공통 검증기를 그대로 호출하지 않고 현재 자료형을 검증한다. 사용자 확정 사항인 관점 2개·4개 세대 코드·조건부 영향 통일을 적용한다.

## 구현 범위 및 방법 (2026-09-20 수정)

사용자 수정 direction.md에 따라 다음 네 항목만 수행한다.

1. 필수값·데이터 형식·허용 DB 코드·점수 범위를 규칙으로 검증한다. 관점 2개, 세대 코드, 용어 최대 3개, 기사 참조와 계산값 등 기존 데이터 계약은 유지한다.
2. 규칙을 통과하면 생성된 문장만 AI에 전달해 과하게 공격적이거나 편향된 표현을 검사한다. 사실 일치·내용 간 모순·일반 UX 라이팅·독립 취재 근거 수는 AI 검증 대상에서 제외한다. 인용된 비판이나 부정적인 사건 자체는 실패 사유가 아니다.
3. 실패한 필드와 반드시 연결된 필드만 한 번 수정한다. 다시 규칙 및 표현 검사를 수행하고 재실패하면 보류한다. 원본·사실·기사 참조와 정상 필드는 보존한다.
4. 최초/최종 검증 결과, 실패 사유, 수정 필드, 토큰과 추정 비용을 기록한다.

- 표현 검증을 활성화하되 새 `TONE` 실행 모드로 이전 `AI` 및 `RULES_ONLY` 결과를 재사용하지 않는다. 이전 실행 기록은 유지한다.
- 수정 프롬프트는 형식 오류 수정과 표현 수정으로 제한하고 전체 UX 가이드는 넣지 않는다.
- 변경 예정 파일: 아래 목록 및 `validation-tone.migration.ts`, `config/news-validation.json`.
- 완료 조건: 범위 밖 AI 판정 거부, 생성 문장만 검토 입력에 포함, 실패 필드만 수정/재실패 보류, 이전 모드와 DB 실행 분리, 단위 테스트·타입 검사·DB smoke 통과.
- 5단계는 구현·커밋 대상에서 제외한다. 아래 과거 실행 기록은 당시 정책에 따른 결과이다.

## 변경 예정 파일

- `apps/batch/src/validation/`: 타입·규칙·모델·서비스·저장소·실행 기록 마이그레이션
- `prompts/news-pipeline/validation-review.md`, `validation-repair.md` 및 프롬프트 목록
- `scripts/news-validation*.mjs`, `package.json`: 실행·보고서·영속 로컬 DB smoke
- `scripts/news-pipeline-report.mjs`: 4단계 결과와 비용 합산
- `test/unit/newsValidation.test.ts`: 검증·부분 수정·재실패 보류 테스트
- `docs/news-pipeline/tests/stage-4-validation.md`: 테스트 시나리오 및 결과

## 완료 조건

- 규칙 실패 시 표현 검증을 호출하지 않고, 수정 이후에만 다음 검증으로 진행한다.
- 실패 필드만 수정하며, 한 차례 수정 후 재실패한 항목은 보류한다.
- 허용되지 않은 수정·잘못된 기사 참조·허용 목록 밖 코드·범위 밖 점수·데이터 구조를 검증한다.
- 영속 로컬 PostgreSQL에서 저장·재개·중복 실행 방지·소유권을 검증한다.
- 사용자 요청에 따라 새로 재실행한 1~3단계 실제 생성 결과를 대상으로 결과보고서와 비용을 확인한다.
- 기존 API·worker를 유지한다. 검증 결과의 실제 이슈 저장·공개는 5단계로 남긴다.

## 진행 상황

- 독립 검증 모듈·외부 프롬프트·마이그레이션·실행 CLI·보고서 구현 완료.
- 규칙 검증은 초안을 변경하지 않는다. 수정은 허용된 필드에만 적용하며 원본과 수정 내역을 보존한다. 점수와 사건 시각 파생값은 원본 생성 시각을 기준으로 코드에서 재계산한다.
- 단위 테스트 및 영속 PostgreSQL smoke 통과. 실제 전체 재실행 진행 중.

## 사용자 승인 및 확정 (2026-09-20)
- 독립 취재 근거 2개 조건을 새 4단계에서 제외한다. 2단계 보도 언론사 수 필터와는 별도 정책이다. 기존 공통 worker/API는 변경하지 않는다.
- 2단계 수정 및 1~3단계 전체 재실행 후 4단계를 구현·실행한다.
- 재생성 단위는 최상위 의미 필드(요약 3줄, 관점 배열 등)로 제한한다. 사건 시각/근거, 공통 영향/개별 영향, 관련 세대/분류, 용어/정의는 연결된 필드로 기록하고 함께 검증한다. 재실패는 보류한다.

## 실제 실행 중 판정 보완
- 초기 의미 검증이 공통 조건부 영향·최초 보도 시각 대체·역할 기반 관점·기사 일부에만 있는 사실을 잘못 실패 처리했다. 해당 정책 예외를 프롬프트에 명시했다.
- 3단계가 이미 사건 시각 제안을 기각하고 FIRST_REPORT로 대체했다면 기각된 제안은 감사 기록으로 보존하고 검증 모델에는 null로 제공한다. 실제 채택된 사건/보도 시각과 점수는 규칙으로 재계산 검증한다. 채택되지 않은 제안을 다시 사실로 취급하지 않는다.
- 초기 4단계 실행을 백업하고 4단계만 새로 실행한다. 1~3단계는 재호출하지 않으며 중단된 검증 비용도 합산한다.

## 용어 상한 3개 (사용자 추가 요청)
- 생성 프롬프트·JSON schema·런타임 검증에서 용어를 최대 3개로 제한한다.
- 현재 재실행에서 이미 생성한 결과는 추가 LLM 호출 없이 기존 순서의 앞 3개와 대응하는 설명만 4단계 최종 사본에 남긴다. 원본 3단계 결과와 제거한 용어 목록은 감사 기록으로 보존한다.
- 진행 중 4단계는 체크포인트부터 재개해 완료된 검증을 재호출하지 않는다. 결과보고서는 최종 사본 기준으로 표시한다.

- 추가 입력 보완: 계산 시각·점수는 코드가 검증하고 LLM 입력에서 제외한다. FIRST_REPORT의 사건 시각 필드는 응답 schema에서도 제외한다. 공통 영향은 의미 검증에 한 번만 전달하고 실제 4세대 동일성은 코드가 검사한다. 부분 수정 호출에는 원래 필드 구조를 제공한다.
- 두 번째 중단 실행 01a0bc6d-9eca-70ef-86e2-d92fe98661c1의 53회 호출까지 별도 백업했다. 두 중단 실행 75회 호출 비용을 합산한다. 최종 실행의 용어 제한 반영 중단은 같은 실행에서 체크포인트 재개했다.

## 전체 재실행 및 4단계 실제 결과 (2026-09-20)

- 1단계: 01a0bc5d-ab4e-768a-bba1-a68e8f35db69. 검색어 34개, 후보 68개, 무관/지엽 제외 10개 및 중복 정리 후 46개. 지역 17개 검색어는 각각 최대 1개. 최종 중복 정리의 번호 충돌 1회는 저장 결과에서 재시도했다.
- 2단계: 01a0bc63-5c57-7732-a93b-08a210e781f2. 선정 34개, 기사 부족 12개, 언론사 부족 0개, 기존 이슈 중복 0개. 선정 이슈 모두 관련 보도 언론사 2곳 이상 확인.
- 3단계: 01a0bc65-7d92-74ea-b9bc-c458064ef422. 생성 34개, 본문 부족 제외 0개. 본문 확보 133건, 실패 16건(이슈별 합계).
- 4단계: 01a0bc71-4328-7796-a6d9-7fd834c588f3. 통과 7개(최초 5개, 수정 후 2개), 보류 27개, 수정 시도 29개. 20개에 용어 상한 정리를 적용했다. 모든 최종 사본의 terms/glossary가 각각 최대 3개이며 통과 7개는 최종 규칙 검사를 다시 통과했다.
- 4단계는 독립 취재 근거 2개 조건을 요구하지 않는다. 5단계 저장·공개는 수행하지 않았다.
- 단위 테스트 최종 300개 통과, 통합 테스트 11개 통과/기존 리포트 9개 건너뜀. API·batch 빌드, 타입 검사, 변경 파일 ESLint 및 2·4단계 영속 PostgreSQL smoke 통과.
- 보고서: .local/news-pipeline-results.md. 원본 및 최종 JSON: .local/news-generation, .local/news-validation.

| 구분 | 호출 | 입력 토큰 | 출력 토큰 | 추정 USD |
| --- | ---: | ---: | ---: | ---: |
| 1단계 | 36 | 93,363 | 3,608 | $0.08625825 |
| 2단계 | 45 | 97,123 | 1,581 | $0.07995675 |
| 3단계 | 102 | 742,420 | 43,671 | $0.75333450 |
| 최종 4단계 | 89 | 1,117,853 | 45,054 | $0.96734715 |
| 중단된 4단계 2회 | 75 | 936,086 | 45,187 | $0.87516600 |

- 합계 입력 2,986,845 / 출력 139,101 토큰, **$2.76206265 USD**. 최종 실행만은 $1.88689665이며 검증 보완 과정 중단 2회 $0.87516600을 별도 합산했다. 세금·환율·이전 실행·개발 테스트 제외. 기록된 캐시 할인 적용, 1·2단계 미기록 캐시는 일반 단가 적용.

## 남은 품질 검토

- 보류 27개를 모두 실제 사실 오류로 단정할 수 없다. LLM이 정상 정책이나 선택적 문체 개선까지 실패로 분류하는 과잉 판정이 남아 있다.
- 예: ‘현대건설, 도시정비 수주 2년 연속 10조 돌파’의 최종 title 판정은 이유에서 핵심 사실이 일치하고 명백한 오류가 없다고 설명하면서 실패 항목으로 반환했다. 이 결과를 임의로 통과시키지 않고 HELD와 원문 판정을 보존했다.
- 이해관계자 주체가 모호하거나 분류가 기사와 맞지 않는 등 검토할 만한 지적도 함께 있으므로, 보류 목록의 실제 내용 오류와 검증기 오판을 구분해 사용자 검토가 필요하다.
- 보완을 위해 전체 1~3단계를 추가 재호출하지 않았다. 초기 검증 2회를 보관하고 4단계 입력만 개선해 새로 검증했다. 최종 실행 중 사용자 용어 상한 요청과 출력 한도/일시 외부 오류는 체크포인트에서 재개했다.

## 사용자 최종 승인 (2026-09-20)

- 사용자가 2~4단계 구현 및 실행 결과를 승인했다.
- 승인 대상은 단계 구현과 검토 결과이며, 보류 27개 이슈의 개별 통과·공개 승인을 뜻하지 않는다. 기존 보류 판정과 과잉 판정 관련 기록을 유지한다.

## AI 검증 일시 비활성화 (2026-09-20 사용자 요청)
- 기존 4단계는 규칙 검사 후 AI 의미 검증·1회 재생성을 수행한다. 과잉 보류 때문에 사용자가 AI 검증 비활성화를 요청했다.
- config/news-validation.json의 aiValidationEnabled 기본값을 false로 두고 의미 검증과 AI 재생성을 모두 생략한다. 규칙 실패는 그대로 보류한다.
- 실행 기록은 생성 실행 ID와 검증 모드(AI/RULES_ONLY) 조합으로 구분한다. 기존 AI 실행·수정·비용을 DB와 파일에 보존하고, 같은 3단계 원본을 새 규칙 전용 실행으로 검증한다. 용어 3개 제한은 유지한다.
- 변경 예정: validation 서비스·타입·저장소·모드 마이그레이션, CLI 설정·보고서, 단위 테스트 및 DB smoke.
- 완료 조건: 기본 실행에서 모델 호출 0회, 규칙 오류 보류 유지, 기존 완료 AI 실행을 재사용하지 않는 규칙 전용 실행 및 로컬 DB 재판정 확인.

## AI 검증 비활성화 결과 (2026-09-20)

- config/news-validation.json: aiValidationEnabled=false. 기본 서비스와 CLI에서 AI 검증 및 재생성 모두 비활성화했다. 규칙 실패는 여전히 보류한다.
- AI/RULES_ONLY 모드별 실행을 분리하는 마이그레이션을 적용했다. 기존 AI 실행 01a0bc71-4328-7796-a6d9-7fd834c588f3은 보존됐다.
- 규칙 전용 실행: 01a0bc89-3049-72ee-930a-469486a1c124. 같은 3단계 원본 34개를 검증해 **34개 통과, 보류 0개**. 기존 AI 수정본을 사용하지 않았고 용어/설명 최대 3개를 유지했다.
- AI 호출·입출력 토큰·추가 비용 모두 0. 과거 AI 비용은 보고서에서 이전 실행 비용으로 별도 보존한다.
- 단위 테스트 16개, 타입 검사·batch 빌드·변경 파일 ESLint 통과. DB smoke에서 동일 3단계의 AI/규칙 전용 실행 분리·원본 유지·재실행 재사용을 확인했다.
- 실제 결과 34개에 의미 검증·재생성 기록이 없고 규칙을 모두 통과함을 다시 확인했다.
- 최신 결과: .local/news-pipeline-results.md. 이전 보고서는 .local/archive/before-rules-only에 보관했다.

## 범위 축소 구현 결과 (현재 정책)

- 위 네 항목으로 검증 범위를 변경했다. 표현 검사에는 생성 문장만 전달하고 기사 본문·분류·시각·점수는 전달하지 않는다. 응답 필드와 실패 유형도 TONE으로 제한한다.
- config/news-validation.json에서 AI를 활성화했다. 새 실행은 TONE이며 이전 AI/RULES_ONLY 실행·비용·판정은 보존한다. 기본 서비스의 명시적 비활성 모드도 유지한다.
- 21개 단위 테스트와 영속 PostgreSQL smoke 통과. 기존 AI 실행이 있어도 새 TONE 실행을 만들고, 규칙 전용 실행과도 분리되는 것을 확인했다.
- 실제 34개 이슈의 새 표현 검증은 아직 실행하지 않았다. 이전 34개 규칙 통과 결과를 새 표현 검사 통과로 간주하지 않는다. 이번 자동화 테스트는 모델 응답 대역을 사용해 추가 LLM 토큰·비용이 0이다.
- 5단계 문서는 로컬 미추적 상태로 두고 커밋에서 제외한다.

- 최종 타입 검사·batch 빌드·변경 코드 ESLint 통과.
